"""Paper PDFs — the core feature (store, verify, fetch, attach, import, serve).

Contract: :mod:`alma.application.pdf_schema`. Modules, in dependency order:

- ``store``    — the file tree, staging, DDL and row helpers (no network).
"""
