{
  "name": "alma-browser-connector",
  "version": "0.20.0",
  "private": true,
  "description": "ALMa Firefox/Chrome browser connector — save the current paper to your library.",
  "type": "commonjs",
  "scripts": {
    "test": "node test/extract.test.js && node test/outbox.test.js"
  }
}
